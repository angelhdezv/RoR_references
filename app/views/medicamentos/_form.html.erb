<%= form_with(model: medicamento, local: true) do |form| %>
  <% if medicamento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(medicamento.errors.count, "error") %> prohibited this medicamento from being saved:</h2>

      <ul>
      <% medicamento.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Nombre %>
    <%= form.text_field :Nombre %>
  </div>

  <div class="field">
    <%= form.label :laboratorio %>
    <%= form.text_field :laboratorio %>
  </div>

  <div class="field">
    <%= form.label :gramaje %>
    <%= form.text_field :gramaje %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
